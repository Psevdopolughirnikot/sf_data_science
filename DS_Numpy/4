import numpy as np
def shuffle_seed(array):
    seed1 = np.random.randint(2**32)
    np.random.seed(seed1)
    result = np.random.permutation(array)
    
    
    return result, seed
print((sum([19, 242, 14, 152, 142, 1000]))/6)
